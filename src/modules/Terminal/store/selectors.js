export const bleSelector = (store) => ({
  connect: store.connect,
  disconnect: store.disconnect,
  logs: store.logs,
  clearLogs: store.clearLogs,
  send: store.send,
});

export const bluetoothSelector = (store) => ({
  connect: store.connect,
  disconnect: store.disconnect,
  logs: store.logs,
  clearLogs: store.clearLogs,
  send: store.send,
});
export const webSocketSelector = (store) => ({
  connect: store.connect,
  disconnect: store.disconnect,
  logs: store.logs,
  clearLogs: store.clearLogs,
  send: store.send,
});
