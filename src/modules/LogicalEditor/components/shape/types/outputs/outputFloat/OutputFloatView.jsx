import { OutputView } from "../output/OutputView";

export const OutputFloatView = ({ width, height }) => {
  return (
    <OutputView width={width} height={height}>
      <p>Output float</p>
    </OutputView>
  );
};
